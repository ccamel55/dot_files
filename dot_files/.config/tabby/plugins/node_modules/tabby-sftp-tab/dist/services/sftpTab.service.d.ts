import { BaseTabComponent, AppService, LogService, TabsService, HotkeysService } from 'tabby-core';
import { Profile, ProfilesService, HostAppService, ConfigService, PartialProfile } from 'tabby-core';
export declare class SftpTabService {
    private config;
    private app;
    private profilesService;
    private hostApp;
    private hotkeys;
    private tabsService;
    private logger;
    private sftpTabConfigProvider;
    constructor(config: ConfigService, app: AppService, profilesService: ProfilesService, hostApp: HostAppService, hotkeys: HotkeysService, tabsService: TabsService, log: LogService);
    init(): void;
    findFirstSshProfile(token: any): Profile | null;
    tryAddSftpProfilesTemplateToConfigService(): Promise<void>;
    findSftpProfileTemplate(): Promise<PartialProfile<Profile> | null>;
    openSftpImpl(activeTab: BaseTabComponent | null): Promise<BaseTabComponent | null>;
    opensftp(activeTab: BaseTabComponent | null): void;
}
