import { TerminalDecorator } from '../api/decorator';
import { BaseTerminalTabComponent } from '../api/baseTerminalTab.component';
import { LogService, HotkeysService, PlatformService } from 'tabby-core';
/** @hidden */
export declare class ZModemDecorator extends TerminalDecorator {
    private platform;
    private logger;
    private activeSession;
    private cancelEvent;
    constructor(log: LogService, hotkeys: HotkeysService, platform: PlatformService);
    attach(terminal: BaseTerminalTabComponent): void;
    private attachToSession;
    private process;
    private receiveFile;
    private sendFile;
    private showMessage;
}
