import { DomSanitizer } from '@angular/platform-browser';
import { ConfigService } from '../services/config.service';
import { HomeBaseService } from '../services/homeBase.service';
import { ToolbarButton, ToolbarButtonProvider } from '../api';
/** @hidden */
export declare class StartPageComponent {
    private config;
    private domSanitizer;
    homeBase: HomeBaseService;
    private toolbarButtonProviders;
    version: string;
    constructor(config: ConfigService, domSanitizer: DomSanitizer, homeBase: HomeBaseService, toolbarButtonProviders: ToolbarButtonProvider[]);
    getButtons(): ToolbarButton[];
    sanitizeIcon(icon?: string): any;
    buttonsTrackBy(btn: ToolbarButton): any;
}
