import { ToolbarButtonProvider, ToolbarButton, AppService, HostAppService, HotkeysService } from 'tabby-core';
/** @hidden */
export declare class ButtonProvider extends ToolbarButtonProvider {
    private app;
    constructor(hostApp: HostAppService, hotkeys: HotkeysService, app: AppService);
    provide(): ToolbarButton[];
    open(): void;
}
